name: Build Miatoll Imperial-X
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Maximize Build Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 4096
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential ccache curl flex g++-multilib gcc-multilib git gnupg gperf lib32readline-dev lib32z1-dev liblz4-tool libncurses5-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev libtinfo5 libelf-dev

      - name: Clone Toolchain (Azure Clang)
        run: |
          git clone https://gitlab.com/Panchajanya1999/azure-clang.git --depth=1 $GITHUB_WORKSPACE/toolchain

      - name: Clone Kernel Source
        run: |
          git clone https://github.com/RiteshSahany/kernel_xiaomi_sm6250 --depth=1 $GITHUB_WORKSPACE/kernel

      - name: Set up KernelSU
        run: |
          cd $GITHUB_WORKSPACE/kernel
          curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -

      - name: Inject SUSFS 4.14 Patches
        run: |
          cd $GITHUB_WORKSPACE
          git clone https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android12-5.10 susfs
          cd kernel
          cp ../susfs/kernel_patches/5.y/* ./ 2>/dev/null || true
          cp ../susfs/kernel_patches/fs/* ./fs/ 2>/dev/null || true
          cp ../susfs/kernel_patches/include/linux/* ./include/linux/ 2>/dev/null || true
          echo "CONFIG_SUSFS=y" >> arch/arm64/configs/vendor/miatoll_defconfig
          echo "CONFIG_SUSFS_SUS_PATH_HIDE=y" >> arch/arm64/configs/vendor/miatoll_defconfig
          echo "CONFIG_SUSFS_TRY_UMOUNT=y" >> arch/arm64/configs/vendor/miatoll_defconfig

      - name: Patch NR_CPUS Tracepoint Bug & Missing Scheduler Flags
        run: |
          # Disarm the hardcoded tracepoint blocker
          sed -i 's/#error "Unsupported NR_CPUS for lb tracepoint."/\/\/ Removed tracepoint block/g' $GITHUB_WORKSPACE/kernel/include/trace/events/sched.h
          
          # Inject the missing NOHZ variables into the defconfig so KernelSU's core hooks can compile
          echo "CONFIG_NO_HZ_COMMON=y" >> $GITHUB_WORKSPACE/kernel/arch/arm64/configs/vendor/miatoll_defconfig
          echo "CONFIG_NO_HZ_FULL=y" >> $GITHUB_WORKSPACE/kernel/arch/arm64/configs/vendor/miatoll_defconfig
          echo "CONFIG_NO_HZ=y" >> $GITHUB_WORKSPACE/kernel/arch/arm64/configs/vendor/miatoll_defconfig

      - name: Compile Kernel
        run: |
          cd $GITHUB_WORKSPACE/kernel
          
          export PATH="$GITHUB_WORKSPACE/toolchain/bin:$PATH"
          export ARCH=arm64
          export SUBARCH=arm64
          
          rm -rf out
          
          make O=out vendor/miatoll_defconfig CC=clang CROSS_COMPILE=aarch64-linux-gnu- CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          
          # Added KCFLAGS="-Wno-error" to instantly ignore code warnings globally across the tree
          make -j$(nproc --all) O=out \
            CC=clang \
            CROSS_COMPILE=aarch64-linux-gnu- \
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
            KCFLAGS="-Wno-error"

      - name: Pack with AnyKernel3
        run: |
          cd $GITHUB_WORKSPACE
          git clone https://github.com/osm0sis/AnyKernel3.git --depth=1 AnyKernel3
          if [ -f kernel/out/arch/arm64/boot/Image.gz-dtb ]; then
            cp kernel/out/arch/arm64/boot/Image.gz-dtb AnyKernel3/
          elif [ -f kernel/out/arch/arm64/boot/Image.gz ]; then
            cp kernel/out/arch/arm64/boot/Image.gz AnyKernel3/
          else
            cp kernel/out/arch/arm64/boot/Image AnyKernel3/
          fi
          cd AnyKernel3
          zip -r9 ../ImperialX-Miatoll-KSU-SUSFS.zip *

      - name: Upload Flashable Kernel
        uses: actions/upload-artifact@v4
        with:
          name: ImperialX-Miatoll-KSU-SUSFS
          path: ImperialX-Miatoll-KSU-SUSFS.zip
